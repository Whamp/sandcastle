---
"@ai-hero/sandcastle": patch
---

Support host-first non-interactive execution with `noSandbox()` in `run()` and `createSandbox()`, defaulting host execution to merge-to-head worktree safety while keeping sandboxed execution modes available as secondary execution modes.
