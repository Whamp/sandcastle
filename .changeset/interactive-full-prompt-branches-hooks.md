---
"@ai-hero/sandcastle": patch
---

Upgrade interactive() from tracer bullet to production-grade: full prompt preprocessing pipeline (promptFile, shell expression expansion, {{KEY}} argument substitution), all three branch strategies (head, merge-to-head, branch), onSandboxReady hooks, copyToSandbox for worktree providers, and env resolution.
